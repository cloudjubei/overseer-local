import React from 'react';

const TasksView: React.FC = () => {
  return (
    <div>
      <h1>Tasks</h1>
      <p>This is where the tasks and features list from task 2 would be displayed.</p>
    </div>
  );
};

export default TasksView;
